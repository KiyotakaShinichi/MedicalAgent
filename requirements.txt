pandas==3.0.2
fastapi==0.136.1
uvicorn[standard]==0.46.0
google-genai==2.0.0
groq==1.2.0
python-dotenv==1.2.2
python-json-logger==4.0.0
sqlalchemy==2.0.49
alembic==1.18.4
psycopg2-binary==2.9.12
openpyxl==3.1.5
pypdf==6.15.0
reportlab==5.0.0
pydicom==3.0.2
pillow==12.3.0
numpy==2.4.4
nibabel==5.4.2
scikit-learn==1.8.0
joblib==1.5.3
torch==2.13.0
torchvision==0.28.0
shap==0.51.0
pyyaml==6.0.3
matplotlib==3.10.9
pytest==9.0.3

# True hybrid RAG (dense FAISS + BM25 + sentence-transformers)
# Install all three to enable local_dense_faiss_hybrid_index backend.
# If unavailable, the app falls back to local_sparse_tfidf_bm25_index.
sentence-transformers==5.4.1
setuptools==83.0.0
faiss-cpu==1.13.2
rank-bm25==0.2.2

# Optional external queue/cache adapter; the default local queue remains DB-backed.
redis==8.0.1

# Feature-flagged OIDC/JWKS bearer validation for non-demo identity providers.
PyJWT[crypto]==2.13.0
